<?php/*
      //for the pagination, we need to know how many pages we will need
      /*if($result->num_rows > 9){
        $pages= num_rows/9;
      }      
      $disabled_link = true;
      $link_dis;
      
      echo('<nav aria-label="Page navigation example">'.
            '<ul class="pagination m-0">');
      if($disabled_link){
        $link_dis='disabled';
      }else{
        $link_dis='';
      }
      echo('<li class="page-item '.$link_dis.' ">'.
                '<a class="page-link_ page-link" href="#" aria-label="Previous">'.
                  '<span aria-hidden="true">&lt;</span>'.
                  '<span class="sr-only">Previous</span>'.
                '</a>'.
               '</li>');
      
      
      
      
      
      echo('</ul>'.
           '</nav>');    
           */
 ?>     